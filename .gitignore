# 敏感凭证（企业微信自建应用 secret 等）——勿提交
wecom_config.json

# 运行时生成的状态/账本文件
live_orders.db
live_cron_state.json
rebalance_log.json
data_cache.db
*.pyc
__pycache__/
# pytest 运行产物（缓存 + basetemp 临时目录，测试生成，非源码）
.pytest_cache/
.pytest_*/
nul
.workbuddy/

# 启动器运行产物（后台进程锁/pid、导出目录）——勿提交
terminal.lock
terminal.pid
outputs/

# 数据后端运行产物（pid / 日志）——勿提交
backend.pid
backend_runtime.log
logs/
