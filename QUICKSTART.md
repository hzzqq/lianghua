# 量化小白上手指南

不需要懂参数、不需要写代码，一条命令就能跑完一次完整的量化回测，**并告诉你这个结果可不可信**。

---

## 一、三步上手

### 第 1 步：跑一遍，看效果

```bash
python tools/quickstart.py --demo
```

用内置演示数据跑，不用联网。你会看到四块内容：

| 板块 | 回答什么问题 |
|---|---|
| 回测结果 | 赚了多少、最大亏了多少、交易了几笔 |
| **可信度体检** | 这个结果**能不能信** |
| **样本外验证** | 是**真有效**，还是参数拟合出来的 |
| 下一步建议 | 接下来该看什么 |

### 第 2 步：换成真实股票

```bash
python tools/quickstart.py --symbol 600519.SH --strategy sma_cross
```

- `600519.SH` 是贵州茅台，换成你想要的任意 A 股代码即可
- 断网或数据源不可用时，会**明确告诉你**已降级为演示数据，绝不拿假数据冒充真实行情

### 第 3 步：全程向导（第一次用推荐）

```bash
python tools/quickstart.py
```

跟着提示选就行，**直接回车表示用默认值**。

---

## 二、结果怎么看（这段最重要）

很多人拿到回测结果只看「总收益」，这是最容易踩坑的地方。请务必先看后面两块。

### 1. 可信度体检

系统会检查这些「结果不可信」的信号，并翻译成人话：

| 提醒 | 含义 |
|---|---|
| 样本过短（<60 根 K 线） | 数据太少，任何统计结论都不可靠 |
| 交易过少（<10 笔） | 收益可能被少数几笔偶然交易决定 |
| 佣金/滑点/印花税全为 0 | 收益虚高，高频策略尤其明显 |
| 夏普比率 > 3 | 实盘长期极少见，疑为过拟合或前视 |
| 收益很高但回撤极小 | 「完美策略」假象，通常是出问题了 |
| 年化收益 > 100% | 需回去复核假设 |
| 权益曲线含 NaN/inf | **致命**，结果不可用 |

### 2. 样本外验证

把数据按时间切成两段：前 60% 观察、后 40% 验证。

| 判定 | 含义 | 该怎么办 |
|---|---|---|
| **稳健** | 样本外仍有优势 | 可以进一步研究 |
| **减弱** | 衰减 40%~70% | 仍有效但明显变弱，参数宜保守 |
| **疑似过拟合** | 衰减 >70% 或由正转负 | **别据此投钱**，换更简单的策略 |
| **无明显优势** | 样本内就没赚到 | 样本外数字没有参考价值 |

### 3. 为什么回测好看、实盘亏钱？

三个主因，本框架都做了防护：

**① 前视偏差（最致命）**
用「当时还不知道的信息」做交易决策。比如策略用今天收盘价算信号，却假设按今天收盘价成交——可你只有在收盘那一刻才知道信号，根本来不及下单。

> 本框架默认 **信号延迟 1 期、次日开盘价成交**（实盘可执行）。
> 已实测：一个偷看未来的作弊策略，在前视设置下回测收益 **+643.94%**、最大回撤仅 0.74%，
> 看起来完美无缺；改成正确设置后变成 **-65.07%**。这就是前视偏差的杀伤力。

**② 过拟合**
参数是在这段历史数据上「调」出来的，只拟合了噪声。用样本外验证可以识别。

**③ 零成本假设**
没算佣金、滑点、印花税。真实交易每笔都有成本，高频策略尤其致命。

---

## 三、命令速查

```bash
# 演示数据（断网可跑）
python tools/quickstart.py --demo

# 指定标的 + 策略
python tools/quickstart.py --symbol 600519.SH --strategy sma_cross

# 指定时间区间与初始资金
python tools/quickstart.py --symbol 000001.SZ --start 2020-01-01 --end 2025-12-31 --cash 500000

# 图形化界面（推荐日常使用）
python start.py          # 然后浏览器打开 http://localhost:8510
```

可用策略（向导里带中文说明）：`sma_cross`（双均线）、`rsi_strategy`、`bollinger`（布林带）、
`macd`、`momentum`（动量）、`mean_reversion`（均值回归）。全部策略列表见 README。

---

## 四、术语小抄

| 术语 | 一句话解释 |
|---|---|
| 回测 | 用历史数据模拟「如果当时按这个策略交易，会怎样」 |
| 策略 | 一套明确的买卖规则 |
| 信号 | 策略在每个交易日给出的指令：买入 / 卖出 / 不动 |
| 总收益 | 期末资金相对初始资金涨了多少 |
| 最大回撤 | 历史上从最高点到最低点最惨亏了多少（越小越平稳） |
| 夏普比率 | 每承担一单位波动换来的收益，越高越好，实盘 >3 极少见 |
| 前视偏差 | 用了「当时还不知道的信息」，回测会虚高 |
| 过拟合 | 参数只拟合了历史噪声，换个时间段就失效 |
| 样本外 | 没参与调参的数据段，用来验证策略是否真的有效 |
| 滑点 | 下单价格与预期价格的偏差，真实交易必然存在 |

---

## 五、常见问题

**Q：为什么跑出来是亏的？**
A：正常。多数简单策略在多数标的上并不赚钱，亏损结果比"暴利"更可信。
真看到年化翻倍的回测，先怀疑是不是前视或过拟合。

**Q：演示数据是真实的吗？**
A：不是，是随机生成的，只用来体验流程。真实数据源不可用时，
命令行会**明确标注**「演示数据（非真实行情）」。

**Q：多久的数据够用？**
A：至少 2 年（约 500 个交易日）。少于 60 根 K 线系统会直接告警。

**Q：为什么最后一天的信号没有成交？**
A：这是**正确行为**，不是 bug。信号在 t 日收盘后产生，只能在 t+1 日执行；
如果回测到 t 日为止，t 日产生的信号就落在区间之外了。强行执行它，
等于假设「在信号产生的同一瞬间成交」——那就是前视偏差。
想让最后一条指令生效，数据区间末尾多留一根 K 线即可。

**Q：回测赚钱就能实盘吗？**
A：不能。回测只是必要条件。还需考虑流动性、涨跌停无法成交、
停牌、极端行情等**回测无法完全模拟**的因素。

---

> 以上所有功能均为历史回测与研究工具，**不构成投资建议**。
