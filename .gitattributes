# 锁死换行符，避免各人 core.autocrlf 设置不同导致启动脚本在别的平台上跑不起来。
* text=auto

# .sh 必须 LF：CRLF 会让 shebang 变成 `#!/usr/bin/env bash\r`，
# 在 Git Bash / WSL / Linux 上报 "bad interpreter"。
*.sh text eol=lf

# .bat 必须 CRLF：cmd.exe 执行批处理时按字节偏移回读文件，CRLF 是它的原生约定。
*.bat text eol=crlf

# 二进制资源不做任何换行转换。
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.db binary
*.xlsx binary
